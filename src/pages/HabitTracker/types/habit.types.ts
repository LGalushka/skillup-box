/*
export type Habit = {
  id: string;
  name: string;
  description?: string;
  color: string;
  icon?: string;
  createdAt: string;
  targetDays?: number;
};

export type HabitLog = {
  id: string;
  habitId: string;
  date: string;
  completed: boolean;
  value?: number;
};

export type HabitStats = {
  totalHabits: number;
  activeHabits: number;
  currentStreak: number;
  longestStreal: number;
  completionRate: number;
};
*/
